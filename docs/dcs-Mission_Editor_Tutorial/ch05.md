# Chapter 05 — Triggers + flags + F10 menus (no Lua)

🇬🇧 **English** | 🇪🇸 [Español](ch05.es.md) | 🇸🇪 [Svenska](ch05.sv.md) | 🇫🇮 [Suomi](ch05.fi.md)

Back to index: [README.md](README.md)

## Goal

Make your mission interactive **without scripting**:

- Use triggers (“when X, do Y”).
- Use flags as the mission’s memory.
- Add a small F10 radio menu for the player.

## Vocabulary (minimum)

- **Trigger**: a rule with conditions and actions.
- **Condition**: what must be true (time, in zone, flag value, group dead…).
- **Action**: what happens (message, activate group, set flag…).
- **Flag**: a number you can set/check (like a variable).

## Recipe A: message when player enters a zone

1. Create a trigger zone named `ZONE_TRAINING`.
2. Create a trigger:
   - Condition: player unit **in zone** `ZONE_TRAINING`
   - Action: show message “Entered training area”.

## Recipe B: activate a late‑activated group

1. Ensure your convoy group has **Late Activation** enabled.
2. Trigger:
   - Condition: player **in zone** `ZONE_TRAINING`
   - Action: **Activate group** `RED_CONVOY_01`

## Recipe C: a simple “mission phase” with flags

Use flags to avoid triggers firing forever.

Example:

- Flag `10` = mission started (0/1)
- Flag `20` = objective complete (0/1)

Pattern:

1. Trigger “Start mission” sets flag `10` to 1 and shows message.
2. Objective trigger only works if flag `10` is 1.
3. When objective completes, set flag `20` to 1 and show message “RTB”.

## Recipe D: F10 menu to start the mission

Create an F10 item “Start exercise”:

- Action: set flag `10` = 1
- Action: show message “Exercise started”

Now your mission has a clear “start button”.

## Quick test

1. Run mission.
2. Use F10 → “Start exercise”.
3. Fly into `ZONE_TRAINING`.
4. Confirm:
   - Messages appear once (not spam).
   - Convoy activates when expected.

## Common errors and fixes

- **Messages spam**:
  - Add a flag gate (“only if flag X = 0”, then set it to 1).
- **F10 item not visible**:
  - Ensure it’s added for the correct coalition/group.
- **Group never activates**:
  - Double-check group name and that it’s late activated.

## Mini‑lab (30–45 min)

Build a 3‑phase mission:

1. Phase 1: Start via F10
2. Phase 2: Enter zone → activate convoy
3. Phase 3: Destroy target → message “RTB” + end condition

## Checklist

- I can gate logic with flags.
- I can activate groups at the right time.
- I can guide the player via F10 and messages.

