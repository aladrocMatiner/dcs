# Capítulo 15 — Biblioteca de recetas copiables: lo que más usarás en DCS

🇬🇧 [English](ch15.md) | 🇪🇸 **Español** | 🇸🇪 [Svenska](ch15.sv.md) | 🇫🇮 [Suomi](ch15.fi.md)

Volver al índice: [README.es.md](README.es.md)

Ejemplo del capítulo: [examples/ex15_recipes.lua](examples/ex15_recipes.lua)

## Qué vas a aprender

- Un conjunto pequeño de “recetas” que puedes copiar/pegar.
- Cómo combinar: mensaje + log + scheduler sin bucles peligrosos.
- Cómo hacer scripts con menos errores y más resultados.

## Cómo se usa en DCS

La idea de “recetas” es:

- Tienes piezas pequeñas y fiables.
- Las reutilizas en tus misiones.

En este capítulo encontrarás ejemplos de:

- `say(text)` (mensaje)
- `log(text)` (log)
- `getFlag(n)` (leer flag)
- `scheduleEvery(n, fn)` (repetir seguro)

## Prueba rápida (paso a paso)

1) Carga al inicio: [examples/ex15_recipes.lua](examples/ex15_recipes.lua)
2) Ejecuta.
3) Verás:
   - Un mensaje “recetas cargadas”.
   - En el log, un “tick 10s” cada 10 segundos.

## Errores típicos y solución

- **No se repite el tick**:
  - Revisa que `scheduleEvery` devuelve el siguiente tiempo.
- **Me preocupa el rendimiento**:
  - Usa intervalos razonables (10–30s) al principio.

> **Si te sale error, haz esto:**
> - Quita la parte del scheduler y deja solo `say(...)` + `log(...)`.
> - Cuando eso funcione, vuelve a añadir el scheduler.

## Mini‑lab (20–30 min): tu “kit base” de misión

Objetivo: crear un archivo `my_mission_kit.lua` con tus recetas.

1) Copia `ex15_recipes.lua` a `my_mission_kit.lua`.
2) Cambia:
   - intervalo de 10s a 30s
   - el mensaje inicial a algo tuyo
3) Añade una receta más:
   - `isTrainingOn()` que devuelva `getFlag(10) == 1`
4) En el tick, si training está ON, escribe un log extra.

## Checklist (debe verse en el juego)

- Mensaje de “kit cargado”.
- En `dcs.log`, un tick periódico.
- No hay congelaciones ni spam excesivo.

## Navegación

Anterior: [Capítulo 14](ch14.es.md)
