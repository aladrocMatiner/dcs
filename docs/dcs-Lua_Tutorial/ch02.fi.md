# Luku 02 — Työputki, asennukset ja yleiset DCS Lua‑kirjastot (VS Code)

🇬🇧 [English](ch02.md) | 🇪🇸 [Español](ch02.es.md) | 🇸🇪 [Svenska](ch02.sv.md) | 🇫🇮 **Suomi**

Takaisin hakemistoon: [README.fi.md](README.fi.md)

## Tavoite

Rakentaa käytännöllinen työputki DCS‑tehtävien Lua‑skriptaukseen käyttäen **Visual Studio Codea**, ja saada yleiskuva yleisimmistä DCS‑Lua‑kirjastoista/ekosysteemeistä.

Tässä luvussa on edelleen **vähän koodia**. Päätulos: “tiedän miten työskentelen”.

## Työputki (kirjoita → pakkaa → aja → havainnoi → iterioi)

Ajattele skriptausta silmukkana:

1. **Kirjoita**: muokkaa VS Codessa (nopea palaute, haku, versionhallinta).
2. **Pakkaa**: lisää skriptit (ja kirjastot) ajettavaan tehtävään.
3. **Aja**: käynnistä tehtävä DCS:ssä.
4. **Havainnoi**: varmista että muutos näkyy (viestit, käyttäytyminen, logit).
5. **Iteroi**: säädä yksi asia kerrallaan.

Yleinen aloittelijan virhe on yrittää tehdä “koko logiikka” ennen kuin silmukka toimii. Rakenna silmukka ensin.

## Mitä pitää “asentaa” (ja mitä ei)

### Lua:n ajaminen DCS:ssä

Useimmiten sinun ei tarvitse asentaa Luaa erikseen, jotta voit *ajaa* tehtäväsriptit DCS:ssä.

DCS suorittaa tehtäväsriptauksen omalla sisäänrakennetulla Lua‑ympäristöllään Mission Editorin triggereiden kautta (esim. `DO SCRIPT` / `DO SCRIPT FILE`).

### Mukavampi työskentely (valinnaiset paikalliset työkalut)

Paikalliset työkalut ovat **kirjoittamista** varten, eivät DCS:n sisäistä ajoa varten:

- **VS Code + Lua language server**: diagnostiikka, täydennys, navigointi.
- **Formatteri** (valinnainen): yhtenäinen tyyli ja selkeät diffit.
- **Lua‑tulkki** (valinnainen): pienet offline‑kokeilut.

Jos asennat Lua‑tulkin: käytä sitä “harjoitteluun”. DCS ajaa silti omassa ympäristössään, joten varmista aina pelissä.

## VS Code ‑asetukset (käytännöllisesti)

### 1a) Asenna Visual Studio Code

Lataa se osoitteesta https://code.visualstudio.com (viralliset OS‑ohjeet: https://code.visualstudio.com/docs/setup).

#### Windows

1) Lataa “User Installer” (helpoin) tai “System Installer” (kaikille käyttäjille).
2) Suorita `.exe` ja seuraa ohjattua asennusta.
3) Suositus: valitse asetuksia kuten “Add to PATH” ja “Add ‘Open with Code’ action”.
4) Käynnistä VS Code ja varmista että se toimii (valinnainen: `code --version` terminaalissa).

#### Linux

Valitse distroosi sopiva tapa:

- Vaihtoehto A (Debian/Ubuntu): lataa `.deb` ja asenna (esim. `sudo apt install ./code_*.deb`).
- Vaihtoehto B (Fedora/RHEL): lataa `.rpm` ja asenna (esim. `sudo dnf install ./code-*.rpm`).
- Vaihtoehto C (Snap, yleinen): `sudo snap install code --classic`.

Jos kone on hallittu (työ/koulu), noudata käytäntöä tai virallisia ohjeita.

#### macOS

1) Lataa `.dmg` tai `.zip`.
2) Siirrä “Visual Studio Code.app” kansioon `/Applications`.
3) (Valinnainen) Avaa Command Palette (`Cmd+Shift+P`) ja aja `Shell Command: Install 'code' command in PATH`.

### 1b) VSCodium vaihtoehtona

VSCodium on telemetriaton build samasta editorista. Lataukset ja asennusohjeet: https://vscodium.com.

#### Windows

1) Lataa MSI‑asennuspaketti VSCodium‑sivulta.
2) Suorita se ja viimeistele ohjattu asennus.
3) Varmista että se käynnistyy (valinnainen: `codium --version` jos PATH on asetettu).

#### Linux

- Vaihtoehto A (paketit): lataa `.deb`/`.rpm` VSCodiumilta ja asenna pakettienhallinnalla.
- Vaihtoehto B (Snap): `sudo snap install codium --classic`.

#### macOS

1) Lataa `.dmg`/`.zip` ja siirrä sovellus `/Applications`.
2) (Valinnainen) asenna `codium`‑komento jos haluat käynnistää terminaalista.

### 1c) Asenna Lua‑laajennus / language server

Tavoite: diagnostiikka ja automaattinen täydennys DCS‑skriptien kirjoittamiseen.

1) Avaa Extensions (`Ctrl+Shift+X` Windows/Linux, `Cmd+Shift+X` macOS).
2) Hae: `Lua` tai `Lua Language Server`.
3) Asenna **“Lua”** (Lua Language Server, `sumneko.lua`).
4) Lataa editori uudelleen, jos pyydetään.

Laajennuksen pitäisi tarjota:

- Syntaksivärityksen
- Diagnostiikan
- Automaattisen täydennyksen (language server)

Lisätietoja: https://marketplace.visualstudio.com/items?itemName=sumneko.lua.

Valinnainen (suositus DCS:lle): aseta Lua runtime‑versioksi **Lua 5.1**, jotta diagnoosit ovat lähempänä DCS:n ympäristöä.

### 2) Valitse yksinkertainen kansiorakenne

Esimerkki:

- `scripts/` — oma tehtäväkoodisi
- `libs/` — kolmannen osapuolen kirjastot (MIST/MOOSE jne.)
- `missions/` — ajettava `.miz` (tai muistiinpanot)
- `notes/` — suunnittelumuistiinpanot ja TODO:t

Tehtävä on “runtime‑kontti”; kansio on “source of truth”.

### 3) Muotoilu (tasainen ja tylsä)

Pidä sisennys ja tyyli johdonmukaisena. Tavoite:

- Selkeät diffit
- Vähemmän “näkymättömiä” virheitä
- Helpompi yhteistyö

Jos käytät formatteria, vältä suuria muotoilumuutoksia samaan aikaan logiikkamuutosten kanssa.

## DCS‑API vs yhteisön kirjastot

Kaksi maailmaa:

### DCS:n sisäänrakennettu skriptaus

Peruspalikat:

- Tapahtumat
- Ajastimet / säännölliset tarkistukset
- Viestit
- Pääsy tehtävän objekteihin (yksiköt, ryhmät, koalitiot, vyöhykkeet) DCS:n sallimissa rajoissa

### Yhteisön kirjastot / frameworkit

Kolmannen osapuolen Lua‑paketteja, jotka auttavat välttämään toiston:

- Korkeamman tason abstraktiot (tasking, spawning, AI‑mallit)
- Aputoiminnot (haku, tila, ajoitus)
- Uudelleenkäytettävät järjestelmät (logistiikka, IADS, pisteytys)

## Yleisiä DCS‑Lua kirjastoja/frameworkeja (käsitteellisesti)

### MIST (Mission Scripting Tools)

Usein utiliittikerros:

- Aputoimintoja ja yleisiä malleja
- Hyvä jos haluat pysyä lähellä “perus DCS‑Luaa” mutta vähentää boilerplatea

### MOOSE (Mission Object Oriented Scripting Environment)

Enemmän framework:

- Auttaa jäsentämään isoja tehtäviä, joissa on paljon liikkuvia osia
- Korkeamman tason käsitteitä ja uudelleenkäytettävää logiikkaa

### Muut add‑onit (kategoriat)

Saatat nähdä myös:

- **IADS‑järjestelmiä**
- **Logistiikka / joukkojen kuljetus**
- **Admin‑työkaluja** moninpeliin

Ota nämä käyttöön vasta kun työputkesi on vakaa.

## Milloin käytät triggereitä vs Luaa vs kirjastoja?

Heuristiikka:

- Yksinkertainen ja staattinen: **Mission Editorin triggerit** riittävät usein.
- Dynaaminen kulku, tila, haarautuminen: **Lua** auttaa.
- Monimutkainen skenaario: **kirjastot/frameworkit** säästävät aikaa, mutta lisäävät ylläpitoa ja riippuvuuksia.

## Checklist: “työputkeni on valmis”

- [ ] VS Code on asennettu ja Lua language server on käytössä.
- [ ] Minulla on kansiorakenne (`scripts/`, `libs/` jne.) ja löydän tiedostot nopeasti.
- [ ] Tiedän miten lisään skriptit tehtävään (`DO SCRIPT` / `DO SCRIPT FILE`).
- [ ] Minulla on tapa varmistaa muutokset (viestit/logit/havaittava käytös).
- [ ] Tiedän mihin suuntaan jatkan (perus Lua, MIST, vai MOOSE).

## Seuraavaksi

Seuraavassa luvussa kirjoitamme pienimmät hyödylliset skriptit ja rakennamme luotettavan “muutos → ajo → varmistus”‑silmukan minimaalisella koodilla.

## Navigointi

Edellinen: [Luku 01](ch01.fi.md)
