# Capítulo 07 — Tablas como configuración (CFG): tu misión “se ajusta” sin tocar lógica

🇬🇧 [English](ch07.md) | 🇪🇸 **Español** | 🇸🇪 [Svenska](ch07.sv.md) | 🇫🇮 [Suomi](ch07.fi.md)

Volver al índice: [README.es.md](README.es.md)

Ejemplo del capítulo: [examples/ex07_cfg_table.lua](examples/ex07_cfg_table.lua)

## Qué vas a aprender

- Qué es una “tabla” en Lua (sin teoría larga).
- Cómo usar una tabla `CFG` para configurar una misión.
- Por qué esto evita errores y te hace más rápido.

## Cómo se usa en DCS

En DCS, una tabla `CFG` es perfecta para:

- Nombres de grupos (`"TGT_01"`, `"TGT_02"`)
- Duración de mensajes
- Textos del menú F10
- Intervalos de timers

La idea es simple:

- **CFG**: lo que cambias (config)
- **Lógica**: lo que hace la misión

## Prueba rápida (paso a paso)

1) Carga: [examples/ex07_cfg_table.lua](examples/ex07_cfg_table.lua) con `DO SCRIPT FILE`.
2) Ejecuta la misión.

Luego, abre el archivo y cambia:

- `messageDuration = 10` por `messageDuration = 3`
- `welcome = "..."` por tu texto

Vuelve a ejecutar.

## Errores típicos y solución

- **Escribo `CFG.welcome` y no funciona**:
  - Revisa que creaste `local CFG = { ... }` arriba.
  - Revisa comas dentro de la tabla.
- **Error por comas o llaves**:
  - En tablas, es común olvidar una coma al final de una línea.

> **Si te sale error, haz esto:**
> - Deja la tabla con solo 1 campo:
>   - `local CFG = { welcome = "Hola" }`
> - Cuando funcione, añade el resto.

## Mini‑lab (15–30 min): “lista de blancos (solo config)”

Objetivo: crear una lista de grupos en `CFG` (aunque todavía no los activemos).

1) Crea `my_ex07.lua`.
2) Pega esto:

```lua
local CFG = {
  targets = { "TGT_01", "TGT_02", "TGT_03" }
}

trigger.action.outText("Targets: " .. table.concat(CFG.targets, ", "), 10)
```

3) Ejecuta y confirma que ves los nombres.

## Checklist (debe verse en el juego)

- Sale un mensaje con “Targets: TGT_01, TGT_02, TGT_03”.
- Cambias la lista y el mensaje cambia sin tocar la lógica.

## Navegación

Anterior: [Capítulo 06](ch06.es.md)  
Siguiente: [Capítulo 08](ch08.es.md)
