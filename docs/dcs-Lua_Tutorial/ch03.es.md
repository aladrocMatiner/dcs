# Capítulo 03 — Bienvenida (Cap 0): tu primer éxito con Lua en DCS

🇬🇧 English (pendiente) | 🇪🇸 **Español** | 🇸🇪 Svenska (pendiente) | 🇫🇮 Suomi (pendiente)

Volver al índice: [README.es.md](README.es.md)

Ejemplo del capítulo: [examples/ex03_hello.lua](examples/ex03_hello.lua)

## Qué vas a aprender

- Qué es “hacer que Lua funcione” dentro de una misión de DCS.
- Cómo ver un resultado inmediato (un mensaje en pantalla).
- Un método para practicar sin frustrarte.

## Cómo se usa en DCS

En DCS no “instalas” Lua para la misión. DCS ya trae Lua dentro.

Tú solo tienes que decirle a la misión: “ejecuta este script”.

La forma más fácil (para empezar) es:

- Mission Editor → *Triggers* → acción `DO SCRIPT` (pegar texto Lua)
- Mission Editor → *Triggers* → acción `DO SCRIPT FILE` (ejecutar un archivo `.lua`)

En este capítulo haremos lo más simple: **mostrar un mensaje**.

## Prueba rápida (paso a paso)

Objetivo: ver un mensaje grande en pantalla diciendo que Lua se ejecutó.

### Opción A (la más fácil): `DO SCRIPT` (pegar)

1) Abre DCS → *Mission Editor*.
2) Crea una misión nueva (un avión jugador en pista es suficiente).
3) Ve a *Triggers*.
4) Crea un trigger:
   - **Type**: `MISSION START`
   - **Action**: `DO SCRIPT`
5) Pega esto:

```lua
trigger.action.outText("Hola. Lua está funcionando en esta misión.", 10)
```

6) Guarda la misión.
7) Ejecuta la misión.

### Opción B (mejor para aprender): `DO SCRIPT FILE` (archivo)

1) En el Mission Editor → *Triggers* → `MISSION START`.
2) Añade acción `DO SCRIPT FILE`.
3) Selecciona el archivo: [examples/ex03_hello.lua](examples/ex03_hello.lua)
4) Guarda la misión (esto suele “meter” el script dentro de la `.miz`).
5) Ejecuta la misión.

## Errores típicos y solución

- **No veo el mensaje**:
  - Asegúrate de que el trigger es `MISSION START`.
  - Asegúrate de que pusiste duración `10` (segundos).
- **Error al ejecutar / no pasa nada**:
  - Empieza por `DO SCRIPT` (Opción A). Es lo más “directo”.
  - Si estás usando `DO SCRIPT FILE`, vuelve a seleccionar el archivo y guarda la misión de nuevo.

> **Si te sale error, haz esto:**
> - Reduce a una sola línea (el `outText`).
> - Guarda la misión, ejecuta, y solo después añade más cosas.

## Mini‑lab (10–15 min): tu “botón de vida”

Objetivo: crear tu “botón de vida” para saber siempre que el script está corriendo.

1) Crea un trigger `MISSION START` con `DO SCRIPT`.
2) Pega este mensaje (cámbialo a tu gusto):

```lua
trigger.action.outText("✅ Tutorial Lua: misión cargada", 10)
```

3) Guarda.
4) Ejecuta la misión y mira si aparece.

Esto será tu prueba rápida para los próximos capítulos.

## Checklist (debe verse en el juego)

- Al iniciar la misión, aparece un mensaje en pantalla durante ~10 segundos.

## Navegación

Anterior: [Capítulo 02](ch02.es.md)  
Siguiente: [Capítulo 04](ch04.es.md)

