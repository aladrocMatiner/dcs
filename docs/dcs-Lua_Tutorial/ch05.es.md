# Capítulo 05 — Variables (sin miedo): guardar datos para tu misión

🇬🇧 [English](ch05.md) | 🇪🇸 **Español** | 🇸🇪 [Svenska](ch05.sv.md) | 🇫🇮 [Suomi](ch05.fi.md)

Volver al índice: [README.es.md](README.es.md)

Ejemplo del capítulo: [examples/ex05_variables.lua](examples/ex05_variables.lua)

## Qué vas a aprender

- Qué es una variable (en 30 segundos).
- Por qué en Lua usamos mucho `local` (para evitar líos).
- Tipos básicos: número, texto, verdadero/falso.
- Cómo se usa esto en DCS: mensajes, configuración simple, contadores.

## Cómo se usa en DCS

En una misión, usar variables te permite:

- Guardar “estado” (por ejemplo: intentos, puntos, fase).
- Guardar configuración (por ejemplo: duración de mensajes).
- Evitar copiar/pegar números por todas partes.

En Lua puedes escribir:

- `local nombre = "Piloto"` (texto)
- `local intentos = 1` (número)
- `local modo = true` (booleano)

La palabra `local` significa: “esta variable vive aquí dentro y no ensucia el resto”.

## Prueba rápida (paso a paso)

Objetivo: mostrar un mensaje usando variables.

1) En tu misión de práctica, crea un trigger `MISSION START`.
2) Acción: `DO SCRIPT FILE`.
3) Selecciona: [examples/ex05_variables.lua](examples/ex05_variables.lua)
4) Ejecuta la misión.

Qué verás:

- Un mensaje “Hola, Piloto. Intentos: 1”
- Una línea en `dcs.log` con `attempts=...`

## Errores típicos y solución

- **Veo “nil” o un mensaje raro**:
  - Revisa que pusiste comillas en los textos: `"Piloto"`.
  - Revisa que no escribiste `..` mal. En Lua se concatena con `..`.
- **No sale el log**:
  - Busca `dcs.log` y busca `DCS-Lua-Tutorial`.

> **Si te sale error, haz esto:**
> - Borra todo y deja solo:
>   - `local x = 1`
>   - `trigger.action.outText("x=" .. x, 10)`
> - Cuando eso funcione, añade el resto.

## Mini‑lab (10–20 min): “contador de intentos”

Objetivo: tener un contador de intentos que tú mismo aumentas a mano (por ahora).

1) Copia `ex05_variables.lua` a un archivo nuevo (por ejemplo `my_ex05.lua`).
2) Cambia `attempts = 1` por `attempts = 3`.
3) Cambia `pilotName` por tu nombre.
4) Ejecuta de nuevo.

Extra (opcional): cambia `fuelPercent` y mira cómo cambia el log.

## Checklist (debe verse en el juego)

- En pantalla aparece tu nombre.
- En pantalla aparece el número de intentos que elegiste.
- En `dcs.log` aparece una línea con `attempts=`.

## Navegación

Anterior: [Capítulo 04](ch04.es.md)  
Siguiente: [Capítulo 06](ch06.es.md)
