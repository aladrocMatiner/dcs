# Chapter 14 — Debugging with no drama: see what happens (checklist)

🇬🇧 **English** | 🇪🇸 [Español](ch14.es.md) | 🇸🇪 [Svenska](ch14.sv.md) | 🇫🇮 [Suomi](ch14.fi.md)

Back to index: [README.md](README.md)

Chapter example: [examples/ex14_debug_nil_checks.lua](examples/ex14_debug_nil_checks.lua)

## What you’ll learn

- The most common mission script failures.
- How to prove execution (screen + log).
- How to avoid `nil` crashes (wrong names).
- A short checklist to save hours.

## How it is used in DCS

Most common DCS scripting problems:

- The trigger didn’t fire.
- A group/zone/unit name does not match exactly.
- You’re looking at the wrong log file.
- You executed the script twice and created duplicates.

Best technique:

1) Visible message (`trigger.action.outText`)
2) Log line (`env.info`)
3) `nil` checks before using mission objects (`if not group then ... return end`)

## Quick test (step-by-step)

Goal: show “group exists” vs “group missing”.

1) In Mission Editor, create a group named exactly `TGT_01` (a static vehicle is fine).
2) Load [examples/ex14_debug_nil_checks.lua](examples/ex14_debug_nil_checks.lua) at `MISSION START`.
3) Run.

Second run (controlled failure):

1) Rename the group to `TGT_01A`.
2) Run again.

## Common errors and fixes

- **“Group does not exist” but I can see it**:
  - Check the exact group name (case, spaces).
  - Make sure you’re using the group name, not the unit name inside.
- **Crash / nil error**:
  - You used an object that doesn’t exist.
  - Fix: always check before use.

> **If you get an error, do this:**
> - Add `outText("step 1", 10)` at the top.
> - If you don’t see it, the script didn’t run. Fix the trigger first.

## Mini‑lab (15–30 min): your personal debug procedure

Goal: build a repeatable debugging routine.

1) Create `my_debug.lua`.
2) Paste:
   - `trigger.action.outText("debug: init", 10)`
   - `env.info("[debug] init")`
3) Load it at `MISSION START`.
4) Make one small change and run.
5) If nothing changes, check:
   - Did you save the mission?
   - Is the right trigger firing?
   - Are you running the right `.lua` file?

## Checklist (what you must see in-game)

- You see “debug: init” on screen.
- `dcs.log` contains `[debug] init`.
- Changing the text shows up next run.

## Navigation

Previous: [Chapter 13](ch13.md)  
Next: [Chapter 15](ch15.md)

