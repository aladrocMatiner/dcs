# Kapitel 06 — Strings och snygga meddelanden: `string.format`

🇬🇧 [English](ch06.md) | 🇪🇸 [Español](ch06.es.md) | 🇸🇪 **Svenska** | 🇫🇮 [Suomi](ch06.fi.md)

Till index: [README.sv.md](README.sv.md)

Exempel: [examples/ex06_strings.lua](examples/ex06_strings.lua)

## Det här lär du dig

- Två sätt att bygga text: `..` och `string.format`.
- Varför `string.format` är extra bra i DCS.
- Hur du använder det för skärmmeddelanden och loggar.

## Så används det i DCS

I DCS visar du ofta:

- “Klättra till 3000 ft”
- “Fart: 250 kt”
- “Poäng: 4”

Med `string.format` kan du skriva mallar:

- `"Altitud: %d ft"`
- `"Fuel: %.1f%%"` (använd `%%` för ett bokstavligt `%`)

## Snabbtest (steg för steg)

1) Ladda [examples/ex06_strings.lua](examples/ex06_strings.lua) med `DO SCRIPT FILE`.
2) Kör.

## Vanliga fel och lösningar

- **Fel i `format`**:
  - Kontrollera parenteser.
  - Kontrollera att `%d`/`%.1f` matchar värdena.
- **Jag ser inget `%`**:
  - Använd `%%`.

> **Om du får fel, gör så här:**
> - Testa först:
>   - `local t = string.format("x=%d", 1)`
>   - `trigger.action.outText(t, 10)`

## Mini‑lab (10–25 min): “målmeddelande”

1) Skapa `my_ex06.lua`.
2) Klistra in och ändra:

```lua
local speed = 250
local altitude = 3000
local text = string.format("Mål: %d kt / %d ft", speed, altitude)
trigger.action.outText(text, 10)
```

## Checklista (ska synas i spelet)

- Meddelandet visar två tal och enheter.
- Inget `nil` och inga `%d` som text.

## Navigering

Föregående: [Kapitel 05](ch05.sv.md)  
Nästa: [Kapitel 07](ch07.sv.md)

