# Kapitel 03 — Välkommen (Cap 0): din första Lua‑vinst i DCS

🇬🇧 [English](ch03.md) | 🇪🇸 [Español](ch03.es.md) | 🇸🇪 **Svenska** | 🇫🇮 [Suomi](ch03.fi.md)

Till index: [README.sv.md](README.sv.md)

Exempel: [examples/ex03_hello.lua](examples/ex03_hello.lua)

## Det här lär du dig

- Vad det betyder att “Lua fungerar” i ett DCS‑uppdrag.
- Hur du får ett direkt resultat (meddelande på skärmen).
- Ett övningssätt med noll stress och låg frustration.

## Så används det i DCS

I DCS behöver du inte “installera Lua” för uppdrag. DCS har Lua inbyggt.

Du behöver bara säga till uppdraget: “kör det här skriptet”.

Två enkla sätt:

- Mission Editor → *Triggers* → `DO SCRIPT` (klistra in Lua‑text)
- Mission Editor → *Triggers* → `DO SCRIPT FILE` (kör en `.lua`‑fil)

I det här kapitlet gör vi det enklaste: **visa ett meddelande**.

## Snabbtest (steg för steg)

Mål: se ett tydligt meddelande som bevisar att Lua kördes.

### Alternativ A (enklast): `DO SCRIPT` (klistra in)

1) Öppna DCS → *Mission Editor*.
2) Skapa ett nytt uppdrag (ett spelarflygplan på banan räcker).
3) Gå till *Triggers*.
4) Skapa en trigger:
   - **Type**: `MISSION START`
   - **Action**: `DO SCRIPT`
5) Klistra in:

```lua
trigger.action.outText("Hej. Lua körs i det här uppdraget.", 10)
```

6) Spara uppdraget.
7) Kör uppdraget.

### Alternativ B (bättre framåt): `DO SCRIPT FILE` (fil)

1) I Mission Editor → *Triggers* → `MISSION START`.
2) Lägg till action `DO SCRIPT FILE`.
3) Välj: [examples/ex03_hello.lua](examples/ex03_hello.lua)
4) Spara uppdraget (DCS paketerar ofta skriptet i `.miz`).
5) Kör uppdraget.

## Vanliga fel och lösningar

- **Jag ser inget meddelande**:
  - Kontrollera att triggern är `MISSION START`.
  - Kontrollera att tiden är `10` sekunder.
- **Fel / inget händer**:
  - Börja med `DO SCRIPT` (Alternativ A).
  - Om du använder `DO SCRIPT FILE`, välj filen igen och spara.

> **Om du får fel, gör så här:**
> - Minska till en enda rad (`outText`).
> - Spara, kör, verifiera, och lägg sedan till mer.

## Mini‑lab (10–15 min): ditt “hjärtslag”

Mål: skapa ett “hjärtslag” som alltid visar att skriptet lever.

1) Skapa `MISSION START` + `DO SCRIPT`.
2) Klistra in:

```lua
trigger.action.outText("Tutorial Lua: uppdrag laddat", 10)
```

3) Spara och kör.

## Checklista (ska synas i spelet)

- Vid uppdragsstart ser du ett meddelande i ~10 sekunder.

## Navigering

Föregående: [Kapitel 02](ch02.sv.md)  
Nästa: [Kapitel 04](ch04.sv.md)

