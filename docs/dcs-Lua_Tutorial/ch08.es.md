# Capítulo 08 — If/Else + Flags: decisiones simples dentro de tu misión

🇬🇧 English (pendiente) | 🇪🇸 **Español** | 🇸🇪 Svenska (pendiente) | 🇫🇮 Suomi (pendiente)

Volver al índice: [README.es.md](README.es.md)

Ejemplo del capítulo: [examples/ex08_if_else_flags.lua](examples/ex08_if_else_flags.lua)

## Qué vas a aprender

- Qué es `if / else` (decidir).
- Qué es un “flag” en DCS (y por qué es tan útil).
- Cómo leer un flag desde Lua: `trigger.misc.getUserFlag`.

## Cómo se usa en DCS

Los flags son el “interruptor universal” del Mission Editor:

- El editor puede activar/desactivar flags por triggers.
- Tu script puede leer flags y tomar decisiones.

Ejemplos reales:

- “Si el alumno ya pasó la fase 1, desbloquea la fase 2”.
- “Si activas modo entrenamiento, muestra ayudas”.

## Prueba rápida (paso a paso)

Objetivo: leer el flag 1 y mostrar un mensaje diferente.

### Paso 1: crea el flag desde el editor (sin código)

1) En *Triggers*, crea un trigger:
   - **Type**: `ONCE`
   - **Condition**: `TIME MORE (5)`
   - **Action**: `FLAG ON (1)`

Esto pone el flag 1 a “ON” después de 5 segundos.

### Paso 2: lee el flag desde Lua

1) Crea otro trigger:
   - **Type**: `ONCE`
   - **Condition**: `TIME MORE (6)`
   - **Action**: `DO SCRIPT FILE`
2) Selecciona: [examples/ex08_if_else_flags.lua](examples/ex08_if_else_flags.lua)
3) Ejecuta la misión.

## Errores típicos y solución

- **Siempre me dice OFF**:
  - Asegúrate de que el trigger del flag ocurre antes (por tiempo).
  - En DCS, los flags suelen ser `0/1` (no “true/false”).
- **Me confunden los nombres**:
  - `trigger.misc.getUserFlag("1")` usa el “nombre” del flag. Aquí es `"1"`.

> **Si te sale error, haz esto:**
> - Prueba con un `DO SCRIPT` que solo haga:
>   - `trigger.action.outText("Flag1=" .. trigger.misc.getUserFlag("1"), 10)`

## Mini‑lab (15–30 min): “modo entrenamiento”

Objetivo: activar y desactivar ayudas con un flag.

1) Crea un flag `10` (en tu cabeza, no hace falta crear nada).
2) Haz un trigger que ponga `FLAG ON (10)` al entrar en una zona.
3) Haz otro trigger que ponga `FLAG OFF (10)` al salir (o a los 2 minutos).
4) Crea un script que lea ese flag y muestre:
   - “Ayudas ON” si es 1
   - “Ayudas OFF” si es 0

## Checklist (debe verse en el juego)

- Al cabo de unos segundos (o al entrar en zona), aparece “ON”.
- En otro momento, aparece “OFF” cuando el flag cambia.

## Navegación

Anterior: [Capítulo 07](ch07.es.md)  
Siguiente: [Capítulo 09](ch09.es.md)

