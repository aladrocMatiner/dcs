# Capítulo 06 — Strings y mensajes bonitos: `string.format` para no sufrir

🇬🇧 English (pendiente) | 🇪🇸 **Español** | 🇸🇪 Svenska (pendiente) | 🇫🇮 Suomi (pendiente)

Volver al índice: [README.es.md](README.es.md)

Ejemplo del capítulo: [examples/ex06_strings.lua](examples/ex06_strings.lua)

## Qué vas a aprender

- Dos formas de crear mensajes: concatenar y `string.format`.
- Por qué `string.format` es tu mejor amigo en DCS.
- Cómo usarlo en mensajes en pantalla y en logs.

## Cómo se usa en DCS

En DCS vas a mostrar muchos mensajes:

- “Sube a 3000 ft”
- “Velocidad: 250 kt”
- “Puntuación: 4”

Si haces todo con `..`, se vuelve feo y propenso a errores.

`string.format` te deja “plantillas”:

- `"Altitud: %d ft"` (número entero)
- `"Fuel: %.1f%%"` (decimal con 1 cifra)

## Prueba rápida (paso a paso)

1) En tu misión de práctica, usa `DO SCRIPT FILE`.
2) Selecciona: [examples/ex06_strings.lua](examples/ex06_strings.lua)
3) Ejecuta.

## Errores típicos y solución

- **Me sale un error con `format`**:
  - Revisa que cerraste paréntesis.
  - Revisa que los `%d`/`%.1f` coinciden con lo que pasas.
- **No sale el símbolo `%`**:
  - En `string.format`, para un `%` literal se usa `%%`.

> **Si te sale error, haz esto:**
> - Empieza con un caso simple:
>   - `local t = string.format("x=%d", 1)`
>   - `trigger.action.outText(t, 10)`

## Mini‑lab (10–25 min): “mensaje de checklist”

Objetivo: crear un mensaje claro tipo checklist.

1) Crea un archivo `my_ex06.lua`.
2) Pega esto y cambia los valores:

```lua
local speed = 250
local altitude = 3000
local text = string.format("Objetivo: %d kt / %d ft", speed, altitude)
trigger.action.outText(text, 10)
```

3) Ejecuta y verifica que el mensaje sale limpio.

## Checklist (debe verse en el juego)

- Sale un mensaje con dos números y unidades.
- No aparecen textos raros como `nil` o `%d`.

## Navegación

Anterior: [Capítulo 05](ch05.es.md)  
Siguiente: [Capítulo 07](ch07.es.md)

